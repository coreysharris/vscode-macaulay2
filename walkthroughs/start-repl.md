Run **Macaulay2: Start M2 REPL** from the Command Palette or press `F12`.

The webview REPL starts `M2` inside VS Code and keeps mathematical output in the editor area.

For a standard VS Code terminal session instead, run **Macaulay2: Start M2 Terminal**.
